# 灰汰郎 Design System

最後更新：2026-08-31（Codex）

## 1. 品牌定位

灰汰郎不是冷冰冰的報價平台，也不是以低價吸引點擊的清潔電商。品牌應像一位熟悉居家問題、願意先把事情說清楚的服務顧問。

核心感受：

- 專業但不壓迫
- 溫暖但不幼稚
- 清楚但不制式
- 有效率但不催促
- 以真實流程建立信任，不使用誇大的「神奇前後對比」

對外文字以「我們」與「服務團隊」為主；施工由合作廠商執行時須清楚說明安排角色。

## 2. 視覺氣氛

設計融合溫暖的居家編輯感、服務業的可信任感與精準的資訊層級。避免常見 AI 網站特徵：大面積霓虹漸層、玻璃擬態、過量 emoji、每張卡片都帶陰影、空泛的「智慧解決方案」文案。

圖片原則：

- 優先使用真實案例；教育流程可使用 AI 輔助的寫實編輯插畫。
- AI 輔助圖片不得冒充實際施工案例；對外統一標示「簡易流程說明」，並在內部資產紀錄保留來源。
- 工具、保護措施與操作姿勢必須合理。
- 不呈現誇張髒污、災難式漏水或恐嚇情境。
- 圖片內不放文字，標題與步驟由 HTML 呈現，確保手機可讀與 SEO。

### 同業操作參考

可參考合作夥伴洗洋洋官網的資訊順序：首屏快速理解服務與價格、服務分類、流程 Step、真實案例、FAQ、LINE 預約。灰汰郎只採用操作邏輯，不複製其品牌配色、圖片、版型或文案。

灰汰郎保留的差異：

- 主導覽維持六大服務，不展開成大量分類選單。
- 每頁先提供問題判讀與服務邊界，不只呈現促銷價格。
- 不使用無證據的健康、殺菌、保固、最低價或好評數字宣稱。
- 聯絡入口維持 LINE，不恢復公開電話 CTA。
- 流程固定以確認、斷電／停水、保護、處理、復原、測試為主。

## 3. 色彩

| Token | 色碼 | 用途 |
|---|---|---|
| `--ink` | `#17324D` | 標題、主要導覽、深色區塊 |
| `--ink-deep` | `#10273B` | Footer、深色互動狀態 |
| `--orange` | `#F28C28` | 主要 CTA、價格重點、品牌識別 |
| `--orange-dark` | `#D96F0D` | CTA hover/active |
| `--teal` | `#138A80` | 完成、安心、檢測與服務狀態 |
| `--line` | `#06C755` | 僅限 LINE 官方操作 |
| `--cream` | `#FFF9F1` | Hero 與溫暖背景 |
| `--sand` | `#F3E8D8` | 分隔、柔和底色 |
| `--paper` | `#FFFFFF` | 卡片與內容底 |
| `--text` | `#263238` | 內文 |
| `--muted` | `#667680` | 次要文字 |
| `--border` | `#DCE4E7` | 邊框 |

規則：單一畫面最多使用一個主 CTA 色。LINE 綠只代表「開啟 LINE」，不能成為通用成功色。

## 4. 字體與層級

主要字體：`Noto Sans TC`。暫不依賴額外襯線字體，避免載入與跨裝置差異；品牌編輯感由字級、字重與行寬建立。

| 樣式 | 桌機 | 手機 | 字重 |
|---|---:|---:|---:|
| Display | 56px | 38px | 900 |
| H1 | 48px | 34px | 900 |
| H2 | 34px | 28px | 900 |
| H3 | 22px | 20px | 800 |
| Body L | 18px | 17px | 400 |
| Body | 16px | 16px | 400 |
| Label | 13px | 13px | 700 |

- 標題行高 1.15–1.3；內文行高 1.7–1.9。
- 內文單行最大約 62 個中文字元。
- 不以全形驚嘆號或連續粗體製造壓迫感。

## 5. 間距與版面

使用 4px 基準：`4 / 8 / 12 / 16 / 24 / 32 / 48 / 64 / 96`。

- 內容最大寬度：1200px。
- 閱讀內容最大寬度：760px。
- 首頁與六服務一般章節垂直留白：`clamp(36px,4vw,60px)`；不再連續疊加 80–104px。
- 同主題內容間距 12–24px；圖片／正文雙欄保留 40px 以上閱讀間距。不可為縮短頁面而壓縮字級或截字。
- 手機左右安全邊距：20px；桌機：32px。
- 卡片內容邊距：24–32px。

首頁每個區塊只回答一個問題：這是什麼、能解決什麼、怎麼做、多少錢、為何可信、如何開始。

## 6. 元件

### 按鈕

- Primary：品牌橘底、白字、14px 圓角；用於「開始估價／預約」。
- LINE：LINE 綠底、白字；文字必須明確寫「LINE 諮詢」。
- Secondary：透明或白底、深藍字、至少 1.5px 邊框並帶低對比陰影。
- 高度至少 48px，手機觸控區至少 44×44px。
- Hover 只做顏色、陰影和 1–2px 位移，不做劇烈縮放。

### 卡片

- 圓角 20–24px。
- 預設使用邊框，不依賴陰影切割資訊。
- 同一區塊最多一層卡片嵌套。
- 卡片 hover 不應隱藏必要資訊。
- 同服務以專屬主色混白 7%／13% 作淺、深兩層區塊底色，與白底交替；價格卡用 4%／9%，避免喧賓奪主。搭配標題、編號與邊界，不能只靠顏色區分主題。
- 資訊說明改短條目／流程清單，不把每段都做成等高大卡。相鄰條目可交替色階；非連結不要假裝可點。
- 實拍案例不改照片色調或裁掉證據；色階只用在外框與說明區。

### 表單

- Label 永遠顯示，不以 placeholder 代替。
- 欄位高度至少 48px。
- 錯誤訊息說明如何修正，不只顯示紅框。
- 預約前說明資料用途與下一步。
- 六大服務不得使用單一原生下拉選單；使用 2 欄服務卡，顯示對應 SVG、名稱與明確選取狀態。
- 日期與時段可以保留原生控制，維持手機系統操作習慣。

### 導覽與圖示

- 2026-08-30 業主重新確認：主導覽固定六項（冷氣清洗／洗衣機清洗／居家清潔／水塔清洗／水管清洗／漏水檢測與修補），取代先前四項決策。手機與平板使用單列橫向滑動，至少露出下一個服務入口；作用中頁籤自動置中。水塔／水管各自選取，不標成漏水頁。
- 桌機 Header 採單列七入口：Logo 即首頁入口，與六個服務頁籤同列並置中對齊；服務文字為主要辨識、圖示為輔，寬螢幕需能立即切換。
- Header 固定於視窗頂部，捲動內容時 Logo 與六個頁籤不可離開畫面；使用等高 spacer 與 `ResizeObserver` 同步實際高度，不以延遲 padding 猜測內容起點。
- 六服務採專屬色彩並延伸至頁面 CTA 與重點區：冷氣青藍、洗衣機紫、居家清潔琥珀、水塔亮藍、水管深青、漏水檢測青綠；非作用中頁籤仍保留自己的圖示色，作用中頁籤使用完整底色。
- **廠商招募入口（2026-08-31 業主確認，取代先前「公開前台不顯示」的決定）**：
  「加入我們 · 廠商合作申請」只能出現在**該頁最後一個轉換 CTA 之後、`<footer>` 正上方**，
  由 `header.js` 統一注入指向 `/join.html`。樣式為深底單行文字連結，
  **不得做成主 CTA 按鈕、不得上移到 Hero／價格區／案例區**等主要轉換路徑。
  實測位置：全站約在 91–92% 捲動深度。
- **浮動聯絡區為「LINE 主、填單估價次」兩個動作**（2026-08-31，取代先前「只保留 LINE」）：
  LINE 使用官方 PNG **直接作為按鈕表面**——容器不套白底、不加邊框，
  否則會出現「白框包綠框」的雙框；仍不另疊一行 LINE 字樣。
  填單估價降為次操作：白底細框、**尺寸與圓角與 LINE 鈕一致（60×60、18px），兩顆在同一條右緣上對齊**，
  文字拆成「填單／估價」兩行，副標移到 `aria-label`（畫面不得再出現「先填需求」這類第三行）。
  **外層容器只當版面用，不得有底色、邊框或陰影**（否則變成卡片包卡片，違反下方卡片規則），
  **全區只保留 LINE 綠一個實色**（違反「單一畫面最多一個主 CTA 色」的是第二個填滿的品牌色）。
  DOM 順序＝視覺順序＝Tab 順序（估價鈕在前、LINE 在後）。1023px 以下浮動區隱藏，由底部固定列接手。

### 聯絡與需求表單

- 頁面內容不可常駐大型「選擇服務／快速估價器」；只在客戶點擊聯絡、預約或估價 CTA 後開啟共用 modal。
- 2026-08-30 業主指定例外：首頁增加「清洗知識／價格一覽」雙入口，預設知識；客戶主動點價格入口後可跨服務選數量、查看小計並複製到 LINE。此試算只存在首頁價格子頁，不恢復各服務頁常駐表單。需報價不能當作零元併入總價，供應商成本不得對外顯示。
- Modal 依目前頁面預選服務；機型與數量預設收合且屬選填，客戶展開後才放入最常見項目並可調整類型、數量或新增項目。
- 姓名、電話、服務為送出必填；服務地區可先填到縣市區，完整地址於確認預約前補齊，降低首次聯絡阻力。
- 數量使用減號、數字、加號控制；每個新增項目可獨立移除。冷氣室外機清洗明示為加購 `$500／台`。
- 圖示使用同一組 24px 線性 SVG：冷氣、洗衣機、住宅清潔、水塔、水管、漏水檢測；不得再用不一致的 emoji 代替服務類型。
- 圖示以 `/assets/icons.svg` sprite 為單一來源，使用 `currentColor` 與低透明填色形成 duotone；小尺寸保持 24×24 光學一致，不從其他品牌網站擷取資產。
- Header 不另放目的不明的知識按鈕；知識內容整合在六大服務頁的內容模組與內連結。
- Logo 在 360px 手機仍需具品牌可讀性，不可被右側操作壓縮成小圖章。
- 公開聯絡操作以 LINE 為主，不顯示電話 CTA；預約表單仍需蒐集派工聯絡電話。
- Header 本體只使用細邊界，不使用外陰影；服務選項本身可使用邊框與低對比陰影表示可點擊。
- LINE 使用可追溯的官方原始 PNG、保持靜態，不手繪、不另疊一行 LINE 字樣。720px以下所有頁面隱藏右側重複浮鈕，保留底部「LINE 直接問／填單估價」，避免擋住內文；首頁價格模式另維持1023px以下隱藏。其餘尺寸的浮鈕不得遮住固定操作。來源與雜湊見 `docs/CODEX-FINAL-REVIEW-2026-08-30.md`。
- 底部固定列的內容保留空間依實際高度同步，包括 safe-area；不得再寫死 66px。
- 手機系統返回鍵在預約抽屜開啟時，必須先關閉抽屜，不可直接離開頁面。
- 真實案例可使用 range input 前後對比滑桿；必須保留「清潔前／清潔後」文字、鍵盤操作標籤、去識別聲明與成果差異說明。

### 狀態標籤

- 橘：需要選擇或注意。
- 青綠：已確認、可服務。
- 深藍：一般資訊。
- 紅色只用於失敗或高風險，不用於促銷倒數。

## 7. 首頁 UX 順序

0. 固定 Header 下立刻顯示「看價格・試算／看清洗知識」；不必讀完 Hero 才能切換。入口列跟隨瀏覽，實高計入錨點避讓。
1. 知識模式 Hero：一句清楚承諾、導向服務內容、服務情境圖。價格模式隱藏整串知識章節，直接顯示試算。
2. 信任列：價格透明、先確認再施工、合作團隊安排。
3. 服務選擇：六大服務與起價／報價方式。
4. 簡易流程說明：首頁以四階段整合確認、保護、處理、復原與驗收；各服務頁保留更完整的專屬步驟。
5. 聯絡後需求表單：由 CTA 開啟 modal，再選服務、類型、數量與聯絡資料；頁面平時不展示大型估價器。
6. 已去識別的真實前後對照、服務保障及預約疑問；與簡易流程示意分區。
7. 服務地區與最終 CTA。

廣告落地頁（目前沒有獨立落地頁，Google Ads 直接導向首頁與六個服務頁）的「加入廠商」入口
只能放在 footer 正上方，與該頁最後一個轉換 CTA 之間要有明顯區隔；不得放進 Hero、價格區或案例區。
⚠️ 首頁目前只在最終 CTA 下方 158px，是全站最近的一處；服務頁為 398–566px。
若之後發現首頁轉換受影響，優先調整的是這段間距，不是把入口整個拿掉。

## 8. 動效

- 尊重 `prefers-reduced-motion`。
- 進場動畫 180–320ms，僅使用 opacity/translate。
- 不使用無限脈衝 CTA；必要提示最多播放一次。
- 捲動時固定元件不得遮住表單、Cookie 或系統鍵盤。

## 9. 無障礙與效能

- 文字對比至少 WCAG AA。
- 圖片提供具體 alt；純裝飾圖片使用空 alt。
- 鍵盤 focus 必須可見。
- 流程圖片使用 WebP/AVIF、明確寬高與 lazy loading。
- 首屏圖片可 `fetchpriority="high"`，其餘 `loading="lazy"`。
- 單張流程圖建議低於 200 KB；首頁總圖片預算建議低於 700 KB。
- 手機 360px 寬不得產生水平捲動。

## 10. 文案規則

使用具體語句：

- 「先確認機型與現場條件，再提供報價」
- 「送出後開啟 LINE，由服務團隊確認」
- 「需要專員聯絡協處」

避免：

- 「AI 智慧安排」、「一鍵解決所有問題」
- 「全台第一」、「最低價」、「保證有效」等無證據宣稱
- 假倒數、假名額、假即時預約通知
- 把簡易流程圖說成真實施工案例

## 11. 廣告投放前門檻

- GA4 真實 Measurement ID 已設定。
- `quote_open`、`quote_submit`、`line_click` 可在 DebugView 驗證。
- 每個廣告群組對應單一服務落地頁，不全部導向首頁。
- 價格、服務地區與 LINE 連結一致。
- 手機 LCP、CLS、表單完成率完成基準量測。
- 簡易流程說明與真實案例分區呈現，內部資產紀錄須標記 AI 圖片來源。

## 12-A 客戶端閱讀節奏（2026-08-31）

六個服務頁在 hero 之後依序放三個區塊，順序不可對調：

1. **Key Impact（`.key-impact`）**：三格結論式數字，大字放結論不放標題。
   數字只能用站上已存在的事實（冷氣 40–60 分鐘、洗衣機 1–2 小時、起價、48H），
   不得為了湊三格而編造。
2. **證據列（`.evidence-strip`）**：2–3 張**真實案例照片**（`/cases/` 或 `/cases-clean/`），
   **不得用 `service-photos`、`/og/` 或插畫充數**，並保留「去識別」聲明與指向該頁案例區的錨點。
   把原本埋在 60–90% 捲動深度的證據前移到 9–26%。
3. **客戶時程軸（`.customer-timeline`）**：四步，放在價格區之前——
   LINE 傳照片 → 確認條件與報價 → 安排到府 → 完工共同檢查。

### 48H／72H 的但書（業主 2026-08-31 指示，**不可省略**）

「大台北約 48 小時、其他地區約 72 小時」是既有承諾（`about.html` 與七個地區頁），
但**只要頁面出現這個數字，同一頁就必須同時出現但書**，兩層都要：

- 短但書（`.ki-note`，跟著數字走）：`48／72 小時是一般狀況，非保證；旺季或排程滿檔會較久。`
- 完整但書（`.ct-caveat`，跟著時程軸走）：另外寫明**旺季（農曆年前大掃除、夏季洗冷氣）
  或排程已滿會較久**、**指定夜間或特定時段需另行確認可否配合**、實際時間以專員回覆為準。

原因：夏季旺季真的排不出來，客戶也可能要求夜間配合。
**不得寫成「保證 48 小時」「準時保證」**，已有測試擋住。

### FAQ 標題點名居住情境

不用泛泛的「常見問題」，改成點名讀者身分（租屋族／自住屋主／房東／管委會／樓上樓下鄰居），
讓讀者知道裡面有自己的狀況。

> 借鏡來源：SimplyCarbs `/business` 的 Key Impact 巨大數字、DAY 1–15 導入時程軸、
> FAQ 點名決策角色三招。**不照抄的部分**：該頁的信任區塊是空的佔位符
> （「合作品牌 Logo 持續更新中⋯⋯」），且表單文案寫「5 個欄位、30 秒填完」
> 實際有 14 個欄位、13 個必填——灰汰郎的證據必須是真的，表單也不得謊報欄位數。

## 12. 流程與長文閱讀（2026-08-30）

- 首頁與六服務使用 `assets/process-editorial.css` 的編輯式序列：一張現場照片、清楚編號、短摘要與原生 details；不用整排等高卡片塞滿長文。
- 全文與安全界線必須留在 DOM；摘要不是取代完整流程的承諾。首頁四階段、居家三個預約步驟、其餘五服務六步，不能為了視覺整齊而編造步驟。
- 編號用深色服務色配淺背景。流程正文桌機 1rem、手機 .95rem；不得固定根字級來掩蓋溢出。
- Hero 大標允許自然換行，雙欄子項 `min-width:0`；至少留 40px 間距，960px 以下單欄。
- 同排案例使用同一媒體比例，不拉伸影像。水塔前後比較固定 4:3。
- 六服務實拍案例統一4:3圖框與contain全幅；媒體比例不能被單張inline樣式改回直式或方形。同排標題、說明與正文以subgrid共享列高，降級仍保留全部內容；不靠截字、固定文字高度對齊。
- 案例專用容器最大1180px，左右24px；依可用寬度與字級減少欄數，手機單欄完整呈現。縣市用完整詞組／清單換行，不把「新竹縣」拆成「新竹／縣」。
- 表格按語意詞組換行，不能整表 nowrap；居家／水管 960px 以下呈資料卡。須以實際畫面驗證 th 已隱藏、欄名在資料旁顯示，不能僅用「無水平溢出」宣稱通過。
- 手機頁內目錄採完整標籤的橫滑導覽；以實測表頭及目錄高度避讓錨點。標題、按鈕不要拆成最後一個字孤立。
- 六服務順讀：問題與處理方式 → 流程及限制 → 真實案例 → 價格與預約；價格捷徑仍在目錄第一項，不強迫價格導向客戶順讀。居家服務地區保留於同頁。
- 頁內目錄使用文字＋作用中底線，不再用第二排巨型膠囊按鈕；高度至少44px，窄螢幕僅目錄本身橫滑。原生錨點與返回行為保留，休眠的分層分頁仍不啟用。
- 首頁數量與價格同列，型號獨立一行、限制說明置後。非常窄或大字環境允許換列，控制項仍至少44px。切換模式／服務分類不清空當頁數量；重新載入與 LINE webview 重建頁面不保證保留。
- 首頁標題按「居家問題／先看懂／找對方法／才有用」語意詞組換行；圖說放在圖片下方，不遮住照片或擠到狹小浮卡。
- 短標題使用 `text-wrap:balance`、短說明使用 `text-wrap:pretty` 做漸進改善；不縮小使用者字級、不把整段設成nowrap。CSS宣告存在不能證明所有瀏覽器與所有文字都沒有孤字，須註明實測選擇器與尺寸。
