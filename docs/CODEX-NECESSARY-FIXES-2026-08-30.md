# Claude 建議必要性判定與最小修正

日期：2026-08-30（Asia/Taipei）。Claude 審查基準 `c66b61a`，本次修改基準 `cee9b90`。只改前台必要問題、測試與交接，不修改 Bot、正式 D1、GA 設定、價格或派工。

## 判定

| 建議 | 判定與處理 |
|---|---|
| P1-A 手機浮動LINE遮字 | 採納。375×812、根字級16px，水管頁圖說實際字框與浮鈕交疊44.03px。720px以下隱藏重複浮鈕，保留底部兩CTA；721px以上一般模式不改，價格模式原1023px防護保留。 |
| P2-B 大字孤字 | 部分採納。對仍有問題的四種短標題加balance、三種說明加pretty，未改字級／原文／容器寬度。案例中繼文字已於cee9b90縮成兩個完整詞組，不再重改。 |
| P2-C 手機價目欄名測試缺口 | 採納，只加測試。現行價目樣式正確，不為此更動UI。檢查兩區非第一欄的::before勝出content與display，並加移除content／important隱藏突變。 |
| P2-D 路徑錯誤 | 不採納「直接全部替換」。Codex確有../huitailang-bot；Claude的目錄名不同。兩份交接補清楚參數意義與兩種範例，不重命名、不更動檢查腳本。 |
| Bot A/B、正式流程、service-layer | 本批不動。附件只是指出待辦，沒有完成證據；不能把前台測試通過當成後台修好。 |

## 實際改動

- `assets/uiux-polish.css`：四類標題balance、三類短說明pretty、720px浮鈕隱藏。
- `header.js`：只將polish資源版本更新為`20260830c`，不改任何互動／資料流程。
- 測試增加8案：Header新增3（含2突變）、流程新增5（含3突變）。共56案（12＋28＋16）。原手機首頁價格模式測試保留。
- 欄名測試只解析::before規則，去掉共同偽元素後綴後比對非第一資料欄；第一欄故意不顯示欄名。此窄範圍宣告檢查不是完整CSS／排版引擎，另以瀏覽器核對生成內容。

## 視覺驗證範圍

本機Chrome，不是真機。預覽只接受GET，CSP禁止正式API與分析腳本，未送任何訂單／LINE訊息。

- 大字比較：七頁×375／768／1440px、根字級24px，共21組。逐一量測下列8種class的直接文字節點，排除編號、圖示與標點，檢查最後一行是否只剩一個字：`.svc-title,.process-title,.cc-title,.promise-title,.cta-sub,.craft-case-meta,.service-visual-note,.process-disclosure`。
- 修改後再加七頁375×812、16px，共28組：指定範圍未再量到行尾孤字，無頁面水平溢出、六服務導覽皆保留。375px文件clientWidth為360px（桌面捲軸占15px）。不能延伸宣稱所有正文／所有展開狀態皆零孤字。
- 同寬下浮鈕display為none，不再有該浮鈕的遮字；768／1440一般模式仍為flex。底部LINE與估價入口保留。
- 水管頁補測720／721／1023／1024px：一般模式分別none／flex／flex／flex，六個導覽保留。375px估價視窗開啟後返回，視窗關閉、留在水管頁、焦點回到「填單估價」。
- 居家／水管在375／768px、24px根字級，經實際點擊價格目錄並等待字體完成：共44個非第一欄的生成欄名均與data-label一致且display:block。詳`price-labels.json`。
- 原始量測與截圖在`design-plans/CLAUDE-NECESSARY-2026-08-30/`。`before.json`是21組24px檢查，`after.json`是28組；不同分母不能直接比較全站總數。前圖為瀏覽器視窗輸出、後圖以原生clip輸出，縮放可能不同，不作逐像素差異比較。

## 未驗與後續

回歸門禁：56/56單元測試（含本批5個突變）、結構22頁／33 sitemap、跨repo21品項、舊手機導覽13個突變及modal20個突變均通過。舊突變腳本執行後已還原，未留測試破壞。

iOS／Android／LINE webview、螢幕閱讀器、舊瀏覽器換行降級、正式D1／8秒逾時／GA DebugView／Bot A/B仍未驗或未修。新CSS僅漸進增強文字換行，不能把Chromium的結果套到Safari。

給Claude：請先唯讀對照`cee9b90..目前版本`，按本文件界定範圍複核。尤其檢查手機所有模式浮鈕隱藏、721px與1023px邊界、底部兩CTA、兩種價目欄名、指定短文字的24px換行，以及突變測試真的能攔截回退。不要覆寫已完成的案例／地區修正，也不要因本地資料夾名不同就判成程式錯誤。沒有真機與正式流程證據的項目仍列未驗。
